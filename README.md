# closest-palindrome-and-number-checker
pal.c is a code that will take an integer as an input and find the closest palindrome.
checkstr.c will determine whether the inputted string is a number or not.
